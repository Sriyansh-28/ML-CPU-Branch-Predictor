# Core runtime dependencies for ML-CPU-Branch-Predictor.
# Versions are lower-bounded to keep the project reproducible while allowing
# security patches. Tested against the versions noted in comments.

numpy>=1.24            # tested: 1.26.x
pandas>=2.0            # tested: 2.2.x
scikit-learn>=1.3      # tested: 1.5.x
matplotlib>=3.7        # tested: 3.9.x
torch>=2.0             # tested: 2.3.x  (CPU build is sufficient)
PyYAML>=6.0            # config loading
